name: Build Flutter APK

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          java-version: "21"
          distribution: "temurin"

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.27.4"
          channel: "stable"

      - name: Verify Flutter & Java
        run: |
          flutter --version
          java -version

      - name: Install dependencies
        run: flutter pub get

      - name: Patch flutter_webrtc for Flutter 3.27+ compatibility
        run: |
          FILE=$(find /home/runner/.pub-cache/git/flutter-webrtc-* -name "SurfaceTextureRenderer.java" | head -1)
          if [ -z "$FILE" ]; then
            echo "Error: SurfaceTextureRenderer.java not found"
            exit 1
          fi
          python3 scripts/patch_webrtc.py "$FILE"

      - name: Build release APK
        run: flutter build apk --release

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: together-apk
          path: build/app/outputs/flutter-apk/app-release.apk
          if-no-files-found: error
